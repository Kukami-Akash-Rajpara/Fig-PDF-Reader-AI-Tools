<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Active tab -->
    <item android:color="@color/dark_pink_color" android:state_selected="true" />
    <!-- Inactive tab -->
    <item android:color="@color/nav_menu_color" />
</selector>