<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Selected -->
    <item android:drawable="@drawable/enabled_background" android:state_selected="true" />
    <!-- Default (not selected) -->
    <item android:drawable="@drawable/disabled_background" />
</selector>